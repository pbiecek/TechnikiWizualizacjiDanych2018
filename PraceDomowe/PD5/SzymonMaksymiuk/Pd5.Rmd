---
title: '[TWD] Praca Domowa nr 5'
author: "Szymon Maksymiuk"
date: "20 listopada 2018"
output:
  html_document:
    toc: true


---

```{r setup, include=FALSE}
library(dplyr)
library(ggplot2)
library(grid)
library(RColorBrewer)

dane <- read.csv("drug-use-by-age.csv")
dane <- select(dane, age, alcohol.use)
dane[,3] <- rep(" ", 17)
dane <- dane[1:12,]
```

#Wstêp
Postanowi³em pochwyliæ siê nad danymi dotycz¹cymi spo¿ycia alkoholu w Wielkiej Brytanii. Przedstawiaj¹ one grupy wiekowe oraz to jaki procent osób z danej grupy spo¿ywa alkohol. Wed³ug mnie s¹ to idealne dane do tego eksperymantu, gdy¿ mo¿na ja przedstawiaæ na wiele sposobów, równie¿ takich mog¹cych wprowadziæ w b³¹d. Ni¿ej zaprezentujê dwa wykresy, jeden zrobiony tak aby utrudniæ ¿ycie u¿ytkownikowi, staraæ siê wprowadziæ go w b³¹d, drugi natomiast próbuj¹cy jak najabrdziej rzetelnie przedstawiæ dane. Nastêpnie prosi³em ankietowanych aby odpowiedzieli na 3 takie same pytania, najpierw patrz¹c na gorszy wykres, a potem na lepszy. Wyniki s¹ zdumiewaj¹ce.

##Wykres 1

```{r, echo=FALSE}
p <-  ggplot(data = dane)+
  geom_bar(aes(x=V3, y= alcohol.use, fill = age), stat = "identity")+
  scale_y_continuous(breaks = 0)+
  coord_polar(theta = "y")+
  theme_minimal()+
  scale_fill_brewer(palette="Set3")+
  labs(x="", y="", fill="Grupa wiekowa", title = "Porównanie procentu osób spo¿ywajacych alkohol \n w danych grupach wiekowych")
p
```

Wykres jest klasycznym beznadziejnym i b³êdnym zastosowaniem wykresu ko³owego. Przede wszystkim sugeruje, ¿e nasze dane sumuj¹ siê do pewnej ca³oœci, podczas gdy przecie¿ wcale tak nie jest. Tracimy równie¿ dok³adne dane liczbowe (specjalnie w obu wersjach wykresu pominê podpisy z wartoœciami). Jedyn¹ rzecz¹, do której móg³by byæ przydatny jest porównanie poszczególnych wycinków w celu stiwerdzenia, która z grupa ma wiêkszy odsetek spo¿ywania alkoholu, co z kolei mija siê z celem z innego powodu. Wielkoœci wycinków ko³owych bardzo trudno siê porównuje.

##Wykres 2 
```{r, echo=FALSE}
q <- ggplot(data=dane, aes(x=age, y=alcohol.use))+
    geom_bar(stat = "identity", fill = "steelblue")+
  theme_minimal()+
  scale_color_brewer(palette="Set3")+
  labs(x="", y="",  title = "Porównanie procentu osób spo¿ywajacych alkohol \n w danych grupach wiekowych")


q
```

Drugi wykres zosta³ przygotowany rzetelnie. Zastosowane s³upki idealnie nadaj¹ siê do prównywania  wartoœci, które razem nie stanowi¹ pewnej ca³oœci. W takiej formie równie¿ nie jest najmniejszym problemem stwierdzenie, która kategoria jest najabrdziej liczna.

#Ankieta
##Czêœæ pierwsza
Pytania w tej czêœci odnosz¹ siê do pierwszego, gorszego wykresu

###Pytanie 1
Czy mo¿na stwierdziæ jak¹ czêœæ wszystkich osób spo¿ywaj¹cych alkohol stanowi¹ osiemnastolatkowie?
```{r, echo=FALSE}
dane <- data.frame(cbind(c("Tak, oko³o 10%", "Tak, ale nie mo¿na podaæ dok³adniej wartoœci", "Nie"), c(6, 12, 2)))
colnames(dane) <- c("Kategoria", "Wynik")

dane$Wynik <- as.numeric(as.vector(dane$Wynik))
ggplot(data=dane, aes(x=Kategoria, y=Wynik)) +
geom_bar(stat="identity", width = 0.8, position = position_dodge(width = 0.9), colour="black", fill="steelblue")+
geom_text(aes(label=Wynik), vjust=1.6, color="white",
          position = position_dodge(0.9), size=3.5)+
labs(x="", y="", fill = "")+
ggtitle("Pytanie 1.1")+
theme_minimal()+
theme(plot.title = element_text(hjust = 0.5))
```

Jak widaæ uda³o siê nam zmanipulowaæ odbiorcê. Oczywiœcie nie jesteœmy w stanie tego stwierdziæ, poniewa¿ jak ju¿ wy¿ej wspomnia³em, mamy do czynienia z danymi procentowami, takimi, które nie sumuj¹ siê do ca³oœci. Dodatkowo jak widaæ po legendzie nie mmamy nawet uwzglêdnionych wszystkich grup wiekowych. Niestety (stety) zastosowanie wykresu ko³owego sk³oni³o odbiorców do b³êdnej oceny.

###Pytanie 2
Czy na wykresie s¹ grupy wiekowe o takim samym odsetku osób spo¿ywaj¹cych alkohol? Je¿eli tak to ile jest takich wartoœci, które s¹ wspólne dla kilku grup?
```{r, echo=FALSE}
dane <- data.frame(cbind(c("0", "1", "2", "3", "4", "6", "7"), c(1, 2, 11, 1, 2, 1, 2)))
colnames(dane) <- c("Kategoria", "Wynik")

dane$Wynik <- as.numeric(as.vector(dane$Wynik))
ggplot(data=dane, aes(x=Kategoria, y=Wynik)) +
geom_bar(stat="identity", width = 0.8, position = position_dodge(width = 0.9), colour="black", fill="steelblue")+
geom_text(aes(label=Wynik), vjust=1.6, color="white",
          position = position_dodge(0.9), size=3.5)+
labs(x="", y="", fill = "")+
ggtitle("Pytanie 1.2")+
theme_minimal()+
theme(plot.title = element_text(hjust = 0.5))     
```

Ponownie, dziêki temu, ¿e na wykresie ko³owym trudno porównuje siê dane, uda³o nam siê wprowadziæ odbiorcê w b³¹d. Tak naprawdê, jak widzieliœmy na wykresie s³upkowym, tych wartoœci, zale¿nie od dok³adnoœci, jest 0 lub 1 (wynika to z tego, ¿e ró¿nica pomiêdzy pewnymi trzema grupami jest minimalna)

###Pytanie 3
```{r, echo=FALSE}
dane <- data.frame(cbind(c("22-23", "24-25", "21", "19", "15"), c(9, 5, 4, 1, 1)))
colnames(dane) <- c("Kategoria", "Wynik")

dane$Wynik <- as.numeric(as.vector(dane$Wynik))
ggplot(data=dane, aes(x=Kategoria, y=Wynik)) +
geom_bar(stat="identity", width = 0.8, position = position_dodge(width = 0.9), colour="black", fill="steelblue")+
geom_text(aes(label=Wynik), vjust=1.6, color="white",
          position = position_dodge(0.9), size=3.5)+
labs(x="", y="", fill = "")+
ggtitle("Pytanie 1.3")+
theme_minimal()+
theme(plot.title = element_text(hjust = 0.5))  
```

W odró¿nieniu do poprzednich pytañ, w tym wypadku dobra odpowiedŸ ma najwiêcej odpowiedzi. Jednak¿e fakt, ¿e nie wszyscy odpowiedzieli poprawnie na tak kluczowe i w gruncie rzeczy proste pytanie ponownie pokazuje, ¿e b³êdne kodowanie danych (w typ wypadku uzycie wykresu ko³owego) sprzyja manipulacji.

##Czêœæ druga
W tej czêœæ pytania odnosiæ siê bêd¹ do drugiego, lepszego wykresu.

###Pytanie 1
Czy mo¿na stwierdziæ jak¹ czêœæ wszystkich osób spo¿ywaj¹cych alkohol stanowi¹ osiemnastolatkowie?
```{r, echo=FALSE}
dane <- data.frame(cbind(c("Tak, oko³o 10%", "Tak, ale nie mo¿na podaæ dok³adniej wartoœci", "Nie"), c(3, 4, 13)))
colnames(dane) <- c("Kategoria", "Wynik")

dane$Wynik <- as.numeric(as.vector(dane$Wynik))
ggplot(data=dane, aes(x=Kategoria, y=Wynik)) +
geom_bar(stat="identity", width = 0.8, position = position_dodge(width = 0.9), colour="black", fill="steelblue")+
geom_text(aes(label=Wynik), vjust=1.6, color="white",
          position = position_dodge(0.9), size=3.5)+
labs(x="", y="", fill = "")+
ggtitle("Pytanie 3.1")+
theme_minimal()+
theme(plot.title = element_text(hjust = 0.5))
```

W tym wypadku, ankietowani ju¿ czêœciej wybierali poprawn¹ odpowiedŸ. Pozbawieni b³êdnej sugestii w postaci wykresu ko³owego mieli ³atwiej stwierdziæ co tak naprawdê wykres przedstawia.

###Pytanie 2
Czy na wykresie s¹ grupy wiekowe o takim samym odsetku osób spo¿ywaj¹cych alkohol? Je¿eli tak to ile jest takich wartoœci, które s¹ wspólne dla kilku grup?

```{r, echo=FALSE}
dane <- data.frame(cbind(c("0", "1", "2", "3"), c(3, 14, 2, 1)))
colnames(dane) <- c("Kategoria", "Wynik")

dane$Wynik <- as.numeric(as.vector(dane$Wynik))
ggplot(data=dane, aes(x=Kategoria, y=Wynik)) +
geom_bar(stat="identity", width = 0.8, position = position_dodge(width = 0.9), colour="black", fill="steelblue")+
geom_text(aes(label=Wynik), vjust=1.6, color="white",
          position = position_dodge(0.9), size=3.5)+
labs(x="", y="", fill = "")+
ggtitle("Pytanie 2.2")+
theme_minimal()+
theme(plot.title = element_text(hjust = 0.5))     
```

Tym razem, maj¹c poprawnie wykonany wykres przed oczami, ankietowani niemal bez³êdnie wskazali poprawne wartoœci.

###Pytanie 3
```{r, echo=FALSE}
dane <- data.frame(cbind(c("22-23", "16"), c(19, 1)))
colnames(dane) <- c("Kategoria", "Wynik")

dane$Wynik <- as.numeric(as.vector(dane$Wynik))
ggplot(data=dane, aes(x=Kategoria, y=Wynik)) +
geom_bar(stat="identity", width = 0.8, position = position_dodge(width = 0.9), colour="black", fill="steelblue")+
geom_text(aes(label=Wynik), vjust=1.6, color="white",
          position = position_dodge(0.9), size=3.5)+
labs(x="", y="", fill = "")+
ggtitle("Pytanie 2.3")+
theme_minimal()+
theme(plot.title = element_text(hjust = 0.5))  
```

Niemal wszyscy ankietowani odpowiedzieli poprawnie na to pytanie. Myœlê, ¿e z czystym sumieniem mo¿emy uznaæ odpowiedŸ "16" za outlier.

#Podsumowanie

Na koniec ankiety zada³em trzy pytania na temat tego jak siê ankietowanym pracowa³o z poszczególnymi wykresami.

```{r, echo=FALSE}
dane <- data.frame(cbind(c("Tak", "Nie"), c(3, 17)))
colnames(dane) <- c("Kategoria", "Wynik")

dane$Wynik <- as.numeric(as.vector(dane$Wynik))
ggplot(data=dane, aes(x=Kategoria, y=Wynik)) +
geom_bar(stat="identity", width = 0.8, position = position_dodge(width = 0.9), colour="black", fill="steelblue")+
geom_text(aes(label=Wynik), vjust=1.6, color="white",
          position = position_dodge(0.9), size=3.5)+
labs(x="", y="", fill = "")+
ggtitle("Czy ³atwo by³o odczytaæ wartoœci z wykresu ko³owego?")+
theme_minimal()+
theme(plot.title = element_text(hjust = 0.5))  


dane <- data.frame(cbind(c("Tak", "Nie"), c(12, 8)))
colnames(dane) <- c("Kategoria", "Wynik")

dane$Wynik <- as.numeric(as.vector(dane$Wynik))
ggplot(data=dane, aes(x=Kategoria, y=Wynik)) +
geom_bar(stat="identity", width = 0.8, position = position_dodge(width = 0.9), colour="black", fill="steelblue")+
geom_text(aes(label=Wynik), vjust=1.6, color="white",
          position = position_dodge(0.9), size=3.5)+
labs(x="", y="", fill = "")+
ggtitle("Czy fakt u¿ycia wykresu ko³owego by³ myl¹cy, bior¹c pod uwagê jakie dane prezentuje?")+
theme_minimal()+
theme(plot.title = element_text(hjust = 0.5))  

dane <- data.frame(cbind(c("1", "2"), c(1, 19)))
colnames(dane) <- c("Kategoria", "Wynik")

dane$Wynik <- as.numeric(as.vector(dane$Wynik))
ggplot(data=dane, aes(x=Kategoria, y=Wynik)) +
geom_bar(stat="identity", width = 0.8, position = position_dodge(width = 0.9), colour="black", fill="steelblue")+
geom_text(aes(label=Wynik), vjust=1.6, color="white",
          position = position_dodge(0.9), size=3.5)+
labs(x="", y="", fill = "")+
ggtitle("Który wykres wydaje Ci siê lepszy")+
theme_minimal()+
theme(plot.title = element_text(hjust = 0.5))  


```

Z przedstawionych odpowiedzi jasno wynika, ¿e ankietowanym lepiej siê pracowa³o z wykresem s³upkowym. Bior¹c teraz pod uwagê wszystkie odpowiedzi, mo¿emy stwierdziæ, ¿e w istocie, nieumiejêtne dobierania rodzajów wykresów do danych mo¿e prowadziæ nie b³êdnych odczytów oraz zak³amañ. Niestety czytelnicy wci¹¿ czêsto polegaj¹ na pierwszym wra¿eniu co pokazuje jak bardzo musimy siê mieæ na bacznoœci prezentuj¹c dane.