
sapply(logs, function(x) proton(action = "server", ))

head(logs[logs$login == ])

logs[order(logs$host),]

proton(action = "server", host="193.0.96.13.15")

library(dplyr)

logs %>% groupby(host) %>% summarize(number = )

